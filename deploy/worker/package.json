{
  "name": "receiptflow-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create receiptflow-db",
    "db:migrate": "wrangler d1 migrations apply receiptflow-db",
    "db:migrate:local": "wrangler d1 migrations apply receiptflow-db --local",
    "generate": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "tesseract.js": "^5.1.1",
    "jose": "^5.9.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.78.0",
    "typescript": "^5.6.0"
  }
}